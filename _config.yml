title: MALTOS
description: Modular infrastructure notes for LLM pretraining and post-training systems.
url: ""
baseurl: "/maltos-blog"
markdown: kramdown
highlighter: rouge
kramdown:
  # Leave $...$ math untouched so MathJax (loaded in the layout) renders it.
  math_engine: null
comments:
  enabled: true
  provider: utterances
  repo: xing7code/maltos-blog
  issue_term: pathname
  theme:
    light: github-light
    dark: github-dark

exclude:
  - .DS_Store
